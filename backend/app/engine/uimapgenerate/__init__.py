"""UI Map generation engine (plugin).

This package is intended to host the real UI Map generation pipeline.
It is wired into the backend as an optional engine; when not available,
the backend falls back to a mock generator.
"""

